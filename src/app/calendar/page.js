import PublicCalendarContent from '../../components/PublicCalendarContent';

export const metadata = {
	title: 'Raumplan – Nur Anzeige',
	description: 'Öffentliche Kalenderansicht des Raumplans (Nur-Lesen)'
};

export default function PublicCalendarPage() {
	return <PublicCalendarContent />;
}
